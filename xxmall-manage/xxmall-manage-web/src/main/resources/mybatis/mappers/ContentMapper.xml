<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
    <mapper namespace="per.xxmall.manage.mapper.ContentMapper">
    
    <select id="queryList" resultType="content">
    	select * from xx_content where category_id=#{categroryId} order by updated DESC
    </select>
    	
    </mapper>